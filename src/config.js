// ======================= CONFIGURE ME =======================
// Base URL of the Module 2 backend (backend/main.py). On a phone running
// Expo Go, "localhost" is the phone itself — use this computer's LAN IP
// (e.g. http://192.168.1.20:8000) or the public tunnel URL.
export const BACKEND_URL = 'http://localhost:8000';

// Must match API_SHARED_SECRET in backend/.env; leave '' if the backend
// runs without one.
export const API_KEY = '';
// ============================================================
